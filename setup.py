from setuptools import find_packages, setup

setup(
    name='src',
    packages=find_packages(),
    version='0.1.0',
    description='Mining users from github and then performing the analysis on the users data',
    author='Venkata, Shashi',
    license='MIT',
)
